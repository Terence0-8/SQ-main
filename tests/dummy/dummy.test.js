// Dummy test file to ensure the glob tests/**/*.test.js matches at least one file
// even if other test files are not present or named differently.
const test = require('node:test');

test('dummy test', () => {
    // This test intentionally does nothing but pass.
});